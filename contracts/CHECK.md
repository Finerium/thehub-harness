# Contracts check (AC-FND-03)

Record of the foundation review of `contracts/`: every schema file was validated by a fresh-context reviewer against
the fields, names, types and enums of blueprint section 9, and every file passes the two deterministic checks below.
The blueprint arbitrates; a name, type or enum here that differs from section 9 is a defect of the schema, never of
the blueprint.

## Deterministic checks

| check | command | result |
| --- | --- | --- |
| JSON Schema 2020-12 validity, `$ref` resolution, `$id` uniqueness | `uv run python tools/check_contracts.py` | 16 files, 0 problems |
| Ajv 2020-12 strict compilation and Zod generation | `pnpm contracts:check` (thehub-3v, over `../thehub-harness/contracts`) | 16 schema files compile; 16 Zod modules generated into `src/contracts/generated` |

## Reviewer verdicts

One reviewer per artefact, read-only, returning `{artifact, pass, findings[{severity, what}]}`. Minor findings are
wording and documentation notes; none changes a name, type or enum.

| artefact | pass | blockers | minors | resolution |
| --- | --- | --- | --- | --- |
| `bundle/manifest.schema.json` | yes | 0 | 1 | |
| `entities/document.schema.json` | yes | 0 | 1 | |
| `entities/asset.schema.json` | yes | 0 | 2 | |
| `entities/operations.schema.json` | yes | 0 | 1 | |
| `entities/coverage.schema.json` | yes | 0 | 1 | |
| `entities/drafts.schema.json` | yes | 0 | 2 | |
| `entities/serving.schema.json` | no, then fixed | 1 | 4 | `$ref` to the evidence packet pointed at a file that does not exist; corrected to `../evidence_packet.schema.json#/$defs/EvidencePacket`; re-checked by `check_contracts.py` (0 dangling refs) |
| `evidence_packet.schema.json` | yes | 0 | 3 | |
| `fixtures.schema.json` | yes | 0 | 4 | Ajv strict mode later rejected eight object subschemas that carried `required` without `type: object`; `type: object` added, no field changed |
| `golden_case.schema.json` | yes | 0 | 6 | |
| `rulepack.schema.json` | yes | 0 | 4 | |
| `gateway.schema.json` | yes | 0 | 7 | |
| `team_facts.schema.json` | yes | 0 | 2 | |
| `connectors/edms.schema.json` | yes | 0 | 3 | |
| `connectors/aims.schema.json` | yes | 0 | 4 | |
| `connectors/historian.schema.json` | yes | 0 | 3 | |
| `README.md` | yes | 0 | 8 | one finding named a consumer outside this directory (the app's generated Zod), handled by `pnpm contracts:check` |
| `api.md` | yes | 0 | 2 | |
| `env.md` | yes | 0 | 2 | |
| `deliverables.md` | yes | 0 | 3 | |

## Consumers pinned to these files

- `thehub-3v/src/contracts/generated/*` is generated by `pnpm contracts:gen` and checked for drift by `pnpm contracts:check` in CI.
- `thehub-3v/src/db/schema.ts` imports the generated enums; its reviewer found the same `$ref` defect and the Ajv strict-mode defect recorded above, both closed.
- Python validates every package and bundle artefact with `jsonschema` against the same files (`make contracts`).
