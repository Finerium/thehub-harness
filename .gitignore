# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# environment and secrets
.env
.env.*
!.env.example
.secrets/
CREDENTIALS_FOR_COMMITTEE*

# tooling traces and process files
.claude/
.crown/
CLAUDE.local.md
.DS_Store
*.log

# corpus-derived text: extracted text, chunks, page renders and full spans are rebuilt from a local corpus, never tracked
packages/text/
packages/chunks/
packages/pages/
packages/claims/spans_full.json
bundle/chunks.jsonl
bundle/text/
bundle/pages/
.cache/

# the built bundle (blueprint 9.1) is a release artefact, produced by `make bundle` where the corpus exists (D-17)
bundle/
graphify-out/
