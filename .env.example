# thehub-harness environment (copy to a gitignored env file; never commit values)
# Absolute path of your own copy of the CALIBER 2026 Case 1 corpus: the "Supporting Data/Case 1_ Manufacturing Knowledge Hub" directory.
CASE1_CORPUS=
# Pooled PostgreSQL connection string of a seeded The Hub database; used only by the golden-set runner (Tier B) and the equality test.
DATABASE_URL=
# No provider key: the AG-1 extraction pass runs from thehub-3v through the application's one gateway (D-18) and its reviewed output is committed here.
